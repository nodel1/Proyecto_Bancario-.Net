using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Windows.Forms;
using MD;
using LN;
using LNCliente;
using MD.MDCliente;
using MD.MDActores;
using MD.MDCuenta;

namespace Presentacion
{
    /// <summary>
    /// Clase principal de la aplicación de presentación que contiene el punto de entrada.
    /// </summary>
    internal static class Presentacion
    {
        /// <summary>
        /// Punto de entrada principal para la aplicación. 
        /// Este método se utiliza para inicializar la aplicación y ejecutar el formulario principal.
        /// </summary>
        /// [STAThread]
        static void Main(string[] args)
        {
            /*
            Actualmente está comentado para evitar la ejecución del formulario.
            
            Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);
            Application.Run(new Form1());
            */
        }
    }
}
